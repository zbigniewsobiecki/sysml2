package EnumTest {
    enum def Status {
        enum Pending;
        enum Active;
        enum Completed;
    }

    enum def Priority {
        enum Low;
        enum Medium;
        enum High;
    }
}
